# White Physiotherapy site: handoff for the next chat

Keep this file OUTSIDE the site folder (it is next to `white-physio/`, not inside), so it is never published.
Read it first, then the build brief (`white-physio-build-brief.md`, uploaded with the project).

## State
- **Homepage is finished** (`white-physio/index.html`): header, hero, stats bar, three pathways, conditions (4 programme cards + 5 home-visit cards), virtual programmes, pricing, patient stories, why White, studio teaser, FAQ, fraud notice, final call to action, footer.
- **Home-visit page is finished** (`white-physio/home-service.html`, 18 sections, see the section below). Preview artifact (update this same one if the page changes): https://claude.ai/artifact/3EC6nfgLswJsmG22yd87iy
- **FlexRecovery Studio page is finished** (`white-physio/studio.html`, 9 sections, see the studio section below). Preview artifact (update this same one if the page changes): https://claude.ai/artifact/CrJrTBDn3FqMN7eENZNPJx. **NOW BUILDING: the programme page template as the 2nd condition, Piriformis Syndrome (`/programmes/piriformis-syndrome`), see the section below (hero, quotes and honest truth done). Sciatica comes after it, then the registration modal, then the payment page, then the other conditions by copying the template and changing the content.**
- **Programme pages built (all four):** `/programmes/piriformis-syndrome`, `/programmes/sciatica-lower-back-pain`, `/programmes/cervical-spondylosis` and `/programmes/knee-arthritis`. On Netlify, `programmes/knee-arthritis.html` is served at `/programmes/knee-arthritis`.

## Programme page: Piriformis Syndrome (`white-physio/programmes/piriformis-syndrome.html`), IN PROGRESS
Built from Praise's Therafit screenshots (desktop-mode phone screenshots, 8 + 2). It becomes the **template** for the other conditions: content sits in blocks marked `CONTENT:` in the HTML; copy the file, change only those blocks, the head (title, description, JSON-LD) and the file name. Every wording block is `data-placeholder` (rephrased from Therafit, simple words).
- **Section order on Therafit's Piriformis page (from the screenshots; this is the order to build):** hero -> "does any of this sound like your life" quotes -> "honest truth" (what was tried, real causes, the one way forward) -> free sample video (`#sample-video`) -> testimonials (4 video cards + "more testimonials" link + Register button) -> 30-day phases (3 cards) -> "everything included" (dark section, 8 items) -> "is this right for you" (for / not for) -> pricing card -> money-back guarantee -> FAQ (6 questions) -> "is this a scam?" trust section (6 tiles + team card + phone/email) -> "the decision" + final Register button -> disclaimer footer. **This page has no "Solution" section, no "How it works" steps and no team-bio card**, and the hero has ONE button. Follow the screenshots, not the SNC order in the build brief.
- **Done (Praise's "go" for the first two items):** PG1 hero (`.pg-hero`, deep blue, pink pill, 3-line headline, one pink button to `#sample-video`), PG2 quotes (`#sound-like-you`, `.pg-quotes`, 5 placeholder quotes + 3 closing lines), PG3 honest truth (`#honest-truth`: `.pg-x-list` 6 things tried, `.pg-causes` box with 6 `.pg-arrows`, closing "only way" paragraph).
- **Also done (2nd "go"):** PG4 free sample video (`#sample-video`, `.pg-sample`, wide video card with a "Free sample" tag via `.video-card--tagged` + `.video-tag`, "after watching" box, blue Register pill with `data-register`), PG5 testimonials (`#testimonials`, `.pg-testi`, deep blue, 4 video cards in `.pg-testi-card`, 1 per row on phones, 2 x 2 from 640px, pink Register pill with `data-register`). Video cards take a YouTube link or ID in `data-video-id`. Therafit's "Verified" tags left out (nothing is verified yet); no stars (not on this page in the screenshots). **Missing, needs White:** the "more video testimonials" link under the cards (a TODO comment marks it; do not link anywhere else). The "Real Nigerians. Real results." label and the price / money-back line under the button are placeholders (`data-placeholder`).
- **Also done (3rd "go"):** PG6 phases (`#phases`, `.pg-phases`, three `.pg-phase` cards: days pill, big faded number, goal line, `.pg-ticks` list; pink, pink, blue top borders as on Therafit; 1 per row on phones, 3 across from 960px), PG7 everything included (`#everything-included`, `.pg-included`, deep blue, 8 `.pg-inc` cards with emoji in a pink circle, 2 across from 768px, pink one-price box). `.pg-ticks` is reused by "Is this right for you". **All eight package items (tea and ointment guide, food plan eBook + video, posture video, bone broth guide, smoothie guide, weight loss tea guide, weekly WhatsApp follow-up, guided videos) are Therafit's package: White must confirm each one exists before launch.** Left out: the struck-through "Total value ₦82,000+" line; the box just states the one (placeholder) price of ₦15,000. The physio's name is not used (Therafit's name removed).
- **Also done (4th "go"):** PG8 "Is this right for you?" (`#is-this-for-you`, `.pg-fit`: blue "for you" card + red-tinted "do not register if" card, 6 + 5 rows via `.pg-ticks--lines` / `.pg-ticks--x`, stacked on phones, side by side from 720px), PG9 pricing (`#pricing`, `.pg-price`: one white card with a pink border, big serif price, 9 ticks, blue Register pill with two lines `.btn--stack` and `data-register`), PG10 Zero risk (`#guarantee`, `.pg-guarantee`: round pink "30 day money back" badge, short promise, small print). Left out: ribbon, struck-through price, "50% OFF". **Guarantee wording: dropped "No argument. No long stories" and "hundreds of people"; refund terms are placeholders. White must confirm the guarantee, what counts as "no improvement", how to claim it and who decides.** The clinic-visit cost line in pricing (₦15,000 to ₦30,000, "most people need several visits") is Therafit's figures: White gives its own or removes it. "Bedridden or in a wheelchair" and "plan to pay later, not possible" are Therafit's rules: White confirms.
- **Also done (5th "go", the rest of the page):** PG11 FAQ (`#faq`, `.pg-faq`: the shared `.faq` accordion as white cards with a pink left edge; 6 questions from the screenshots; answers are MY placeholders because Therafit's were not captured, cautious on purpose; **White's physiotherapists must check the health answers**; FAQPage JSON-LD in the head repeats the same text, keep them in step), PG12 "How do I know this is not a scam?" (`#not-a-scam`, `.pg-trust`, deep blue: six evidence tiles, free-sample line, team card with `[Name]` placeholders, phone + WhatsApp line; **every tile is a placeholder claim: CAC registration, licensed physios, testimonials that exist, the guarantee; keep only what is true; White's real team names/roles/photos replace the `[Name]` rows**; Therafit's names, phone numbers and email are not used), PG13 the decision + final Register button (`#the-decision`, `.pg-decision`; left out "One slot remains"). The disclaimer footer is the shared site footer (it already has a disclaimer).
- **THE PROGRAMME PAGE IS FULLY BUILT** (all sections in the screenshot order). **Sticky button: Praise did NOT want the sticky "Register Now" pill. Instead: a small green WhatsApp button, fixed bottom right, on EVERY page** (see "Floating WhatsApp button" under Files). Nothing else is pending on this page except the registration modal and payment page (below) and White's real content.
- **REGISTRATION FORM: DONE (Praise's "build the form" go).** A native `<dialog class="reg" id="register">` at the end of the page (after `</main>`), CSS section 28, JS block at the end of `main.js`. Every `[data-register]` link opens it (also `#register` in the address). Fields: first name*, last name*, WhatsApp number*, email*, how long looking for a solution*, what tried before*, weight category*, confirmation checkbox*; hidden `form-name=registration`, `programme` ("Piriformis Syndrome"), `amount` ("15000"), honeypot `bot-field`. Checks each field in the browser with plain-language messages, then POSTs to Netlify (`fetch('/')`, urlencoded), then goes to `/payment?programme=...&name=...&amount=...`. On failure it shows a message with a WhatsApp link and lets the visitor retry. Without JavaScript the form still posts and Netlify sends the visitor to `/payment` (the form's `action`).
  - **Changes from Therafit (mine, tell Praise if he objects):** first AND last name are required (the payment narration needs the full name); added an "Under 50kg" option (Therafit's weight options start at 50kg, which would block a slim visitor); the confirmation sentence now says payment details show right after the form and WhatsApp access comes once payment is confirmed (Therafit's said access came "immediately after submitting", which contradicts its own payment step); dropped "Only 5 slots remaining" (fake urgency); the four trust claims under the form (secure & private, CAC registered, 30-day money-back, access via WhatsApp) are placeholders.
  - **Per programme (CONTENT):** the title, the `programme` and `amount` hidden values, and the price in the confirmation line.
  - **Netlify steps for Praise (nothing to wire in code):** deploy; in the Netlify dashboard open Forms and "Enable form detection" (needed if not already on, applies from the next deploy); add an email notification for the form there; submit one real test registration. The form name `registration` must stay the same on every programme page so all entries land in one list.
  - **Preview builds:** `tools/build_preview.py` adds `data-form-preview` to the real `<body>`, so in a preview the form shows a "Preview only. Nothing was sent." result instead of posting. The real site never has that attribute.
  - **Data note:** the form collects health-related answers and contact details. There is no privacy policy page yet; worth suggesting to Praise / White before launch.
- **SHARED PAYMENT PAGE: DONE** (`white-physio/payment.html`, route `/payment`; Praise decided ONE page for all programmes; CSS section 29; JS block "Payment page" at the end of `main.js`). Logo-only top bar, `noindex`. It reads `?programme=...&name=...&amount=...&t=...` from the address (set by the registration form) and shows them with `textContent` only: "Thank you, [name]", the exact amount (twice), "30-day [programme] programme", and a "Send receipt on WhatsApp" link with a ready message (name, amount, programme). Anything missing keeps plain fallback wording ("Thank you.", "Confirm on WhatsApp", "Your 30-day programme"), which is what a no-JavaScript submission shows. The amount is only displayed: White always checks the payment itself. In previews (`data-form-preview`) it shows an example (Ada, Piriformis Syndrome, ₦15,000).
  - Sections in order: three-step tracker (form submitted / make payment / receive access), heading, exact amount box, 30-minute hold box, account details (bank, account number with a Copy button, account name, amount) + method pills + red "transfer the exact amount, use your full name" warning, "what happens after you pay" (4 steps), dark "send your receipt" card, "remember why you registered" quote, dark "please remember" card (5 points), guarantee card, help line. The shared footer carries the disclaimer.
  - **PLACEHOLDERS (`data-placeholder`), White's real details replace them:** bank "Moniepoint MFB", account number `0123456789` (MADE UP, in the HTML and in the copy button's `data-copy`: change BOTH), account name "White Physiotherapy Clinic", "POS available", the guarantee card, the patient quote (a typical situation, not a real patient). WhatsApp number/phone come from `SITE` in `components.js` (the help line repeats the number in plain HTML, same as the other pages).
  - **30-MINUTE HOLD: ADDED at Praise's request (he reversed the earlier "deferred" call).** A countdown bar under the header (`.pay-hold`) and a "held for 30 minutes" box above the account details (`.pay-held`), CSS 29f, JS in the Payment block (`HOLD_MINUTES = 30`). Both start `hidden` and only show when the address carries `t=` (the registration form adds `t` = the time it was sent, in ms). The countdown is counted from `t`, so a refresh or coming back from a banking app does NOT restart it (Therafit's restarts per visitor; ours does not). No `t` (no-JavaScript route, direct visit, bad value) = no bar and no box. A `t` in the future is clamped to now. It is display only: at zero the wording changes to "Your 30 minutes are up. Message us on WhatsApp and we will help." and the timer hides, but NOTHING is locked (bank details, amount and receipt button stay). Wording is wrapped in `data-placeholder`: **White must actually hold a registration for 30 minutes, or change the words / `HOLD_MINUTES`.** Previews show the countdown from 30:00 on every load. Still left out: Therafit's fifth "Please remember" point ("slot hold time"), because the bar and box now cover it; and the word "slot" (we say "registration", since there are no limited slots).
  - Preview artifact for this page: its OWN artifact (see the link Praise was given; publish updates to that same one).
- **SCIATICA & LOWER BACK PAIN PAGE: DONE** (`white-physio/programmes/sciatica-lower-back-pain.html`, made from the Piriformis page: same 13 sections in the same order, same components, same registration dialog and same payment flow; Praise pasted Therafit's SNC page text and said keep the Piriformis structure). Preview artifact: its OWN artifact (link given to Praise; update that same one).
  - **How it was made:** copied `piriformis-syndrome.html` and replaced only the `CONTENT:` blocks, the head (title, description, Service + FAQPage JSON-LD), the WhatsApp message (`data-wa-message`), the dialog title and the hidden `programme` value ("Sciatica & Lower Back Pain"; `amount` still 15000). The content is Therafit's SNC wording REPHRASED in simple words and tagged `data-placeholder`. Checked: all 13 section ids match the Piriformis page, every `#anchor` has a target, all Register buttons open the dialog, the FAQPage JSON-LD text equals the visible FAQ, no Therafit names, phone numbers or urgency wording in the visible text.
  - **Differences from the Piriformis content:** 5 real causes (Piriformis has 6); 7 "everything included" items (smoothie and weight-loss tea guides are ONE item, as on Therafit's SNC page; the last card spans both columns on desktop when the count is odd, small CSS rule added); 10 price-card ticks; 6 FAQ questions (Therafit's 7th, "Why is the price ₦15,000 right now and not ₦30,000?", is left out because it belongs to the price countdown).
  - **Left out from Therafit's SNC page on purpose:** the price countdown "window" (₦15,000 then ₦30,000), "slots remaining", "price doubles", struck-through regular price and "50% OFF", "Total value ₦82,000+", the Therafit team names / phone numbers / email, the named patient stories (Alhaji Musa, Mrs Adunola, Chief Emeka), the four trust chips in the hero, "hundreds of people", "No argument". **Not used because the Piriformis structure has none of them:** "The Solution (The SNC Program)", "How It Works" (3 steps) and the team bio card. Add them if Praise asks (build brief section 7 describes them).
  - **Needs White / a physiotherapist before launch (this page):** every health statement, especially the surgery item and the FAQ answers (I added a red-flag safety line to the "afraid to exercise" answer: new leg weakness, numbness around the groin, or trouble controlling bladder or bowels means see a doctor urgently); the "real Nigerians" line under the quotes; all the package items; price, guarantee and the trust tiles (same lists as the Piriformis page).
- **CERVICAL SPONDYLOSIS & SHOULDER PAIN PAGE: DONE** (`white-physio/programmes/cervical-spondylosis.html`; made from the Piriformis page exactly like the Sciatica page: same 13 sections in the same order, same dialog and payment flow; Praise pasted Therafit's Cervical page text). Preview artifact: its OWN artifact (link given to Praise; update that same one). Hidden `programme` value: "Cervical Spondylosis & Shoulder Pain"; `amount` 15000.
  - **Checked:** all 13 section ids match the Piriformis page; counts match the pasted content (6 quotes, 6 "tried" items, 5 causes, phases 5 / 5 / 5 ticks, 8 included items, 7 "for you" / 6 "do not register if", 8 price ticks + guarantee, 6 FAQ); every `#anchor` has a target; all Register links open the dialog; the FAQPage JSON-LD text equals the visible FAQ; no Therafit names, phone numbers or urgency wording in the visible text; the form redirects to `/payment?programme=Cervical+Spondylosis+%26+Shoulder+Pain&...`.
  - **Left out on purpose (also on the other two pages):** the "price increases to 30,000" banner, "Last Slot Warning", "one slot remains", struck-through price and "50% OFF", "Total value ₦82,000+", Therafit team names / phone numbers / email, "hundreds of Nigerians", "No argument". **Also left out here:** the "7 Minutes" line under the video, the "Recovered" / "Drug-Free" tags on the testimonials, the drug brand names (Ibuprofen, Diclofenac, Feldene), and the line telling doctors off ("too many Nigerian doctors..."; the item is now a neutral "It is just ageing, so manage it").
  - **Not used (Piriformis structure has no such section):** the "How You Receive Everything" block. Its facts are folded into the "Everything included" intro line on this page ("sent to your WhatsApp once your payment is confirmed... no special app") and the payment page. Add it as a section if Praise asks.
  - **Needs White / a physiotherapist before launch (this page):** every health statement, especially: the surgery wording, the "4 weeks" fresh injury and "6 weeks" after surgery rules (Therafit's numbers, kept because they are safety rules), the "it is just ageing" item, and the FAQ answers (the "afraid to move my neck" answer has a red-flag line: new arm or leg weakness, trouble walking or balancing, or trouble controlling bladder or bowels means see a doctor urgently; the surgery answer defers to the person's doctor). Also the "real Nigerians" line, the package items, price, guarantee and trust tiles (same lists as the other pages).
- **KNEE ARTHRITIS & JOINT RECOVERY PAGE: DONE** (`white-physio/programmes/knee-arthritis.html`; made from the Piriformis page exactly like the Sciatica and Cervical pages; Praise pasted Therafit's Knee page text). Preview artifact: its OWN artifact (link given to Praise; update that same one). Hidden `programme` value: "Knee Arthritis & Joint Recovery"; `amount` 15000.
  - **Checked:** all 13 section ids match the Piriformis page; counts match the pasted content (6 quotes, 6 "tried" items, 5 causes, phases 5 / 5 / 5 ticks, 9 included items, 8 "for you" / 6 "do not register if", 8 price ticks + guarantee, 6 FAQ); every `#anchor` has a target; all Register links open the dialog; the FAQPage JSON-LD text equals the visible FAQ; no Therafit names, phone numbers or urgency wording in the visible text; the form redirects to `/payment?programme=Knee+Arthritis+%26+Joint+Recovery&...`. With 9 included items the last card spans both columns on desktop (existing CSS rule).
  - **Left out on purpose (also on the other pages):** the price banner, "Last Slot Warning", "one slot remains", struck-through price and "50% OFF", "Total value", Therafit team names / numbers / email, "hundreds of Nigerians", "No argument", the drug brand names, the doctor-blaming line. **Also left out or softened here:** "Without Surgery or Drugs" in the headline (the third headline line is now "And How to Finally Get Relief", same as the other pages), the claims that steroid injections "accelerate joint damage", that the wrong exercise will "grind the damaged cartilage", that broth is "cartilage-healing" and contains "glucosamine", the "4 to 6 times your weight" figure, the "Recovered" / "Drug-Free" testimonial tags. The FAQ answer now says plainly that exercise does not rebuild worn cartilage but stronger muscles can take load off the joint.
  - **Not used (Piriformis structure has no such section):** "How You Receive Everything" (folded into the "Everything included" intro line, as on the Cervical page).
  - **Needs White / a physiotherapist before launch (this page):** every health statement, especially the injections and surgery wording, the "4 weeks" fresh injury and "6 weeks" after surgery rules (Therafit's numbers, kept because they are safety rules), and the FAQ answers (the "afraid it will make it worse" answer has a red-flag line: sudden very swollen, hot, red knee, fever, or unable to put weight on the leg means see a doctor urgently; the surgery answer defers to the person's doctor; the elderly answer suggests a sturdy chair or wall nearby). Also the "real Nigerians" line, the package items, price, guarantee and trust tiles.
- **ALL PAGES ARE BUILT** (homepage, home-service, studio, four programme pages, registration form, payment page). **Remaining before launch (all need White or Praise, no more building is planned):** replace every `data-placeholder` (search the files for it) with White's real content; real videos (`data-video-id`), photos, team, prices, guarantee decision, bank details (payment page: change BOTH the visible account number and the Copy button's `data-copy`), phone / email / WhatsApp in `SITE` (`components.js`); White's physiotherapists to check every health statement on the four programme pages; a privacy policy page (the form collects contact and health details); deploy to Netlify, enable form detection, add the email notification for the "registration" form, and submit one real test registration; add `<link rel="canonical">`, `og:url` and `og:image` once the domain is known (TODO comments mark the spots); then a sitemap / robots check.
- **Top bar:** `<body data-header-logo-only>` gives a logo-only header (`components.js`; `main.js` skips the menu code when there is no menu). Therafit's countdown / "batch" banner above it is left out on purpose; an HTML comment marks the spot.
- **Section labels on programme pages are plain small text (`.pg-label`, dark pink for contrast), headings left-aligned (`.pg-h2`), not the centred pill style of the other pages.** New CSS is section 27 at the end of `style.css`.
- **Left out on purpose (all pages):** countdown/batch banner, "Last Slot Warning" band, "CURRENT BATCH ONLY" ribbon, struck-through price, "50% OFF", the "Total value ₦82,000+" line, "price goes up to ₦30,000 next batch".
- **Money-back guarantee: Praise asked for it (the Therafit "Zero Risk" section = 30-day money back), so it will be built, but every mention is `data-placeholder` and MUST be confirmed with White before launch** (what counts as "no improvement", how to claim, who pays). Places it appears: price card tick, the Zero Risk section with the round "30 day money back" badge, the small line under the Register buttons, the "full refund" tile in the trust section, the registration form trust row, the payment page badge. Drop "hundreds of people" from its text unless true.
- **Placeholder claims to confirm with White (this page so far):** "licensed physiotherapist" (hero), "real Nigerians" line under the quotes (PG2; quotes are typical situations, not real patients), and every health statement in PG3 (drugs / massage / exercises / causes / "lasting relief"): a physiotherapist must check them.
- **Preview:** `python3 tools/build_preview.py white-physio white-physio-piriformis-preview.html programmes/piriformis-syndrome.html`, published as its OWN artifact (not the home-visit or studio one).

## Home-visit page (`white-physio/home-service.html`): DONE (kept here as the reference for how the later pages should look and work)
Built first, before the programme pages, at Praise's request. Sections were built one or two per message, each after his "go".
- **Progress:** HV1 hero (4 buttons: Book, WhatsApp, Watch a Real Home Visit -> `#home-visit-video`, See Patient Stories -> `#patients`), HV2 trust strip, HV3 "does any of this sound familiar" quotes (4 cards, one per row on phones, 2 x 2 from 560px; ends with a Book My Home Visit button) done. HV4 "why drugs, massage and quick fixes keep failing you" (5 cards with red crosses), HV5 "you don't always need to travel" (6 cards) HV6 "what a White home visit looks like" (deep blue, flyer video card `#home-visit-video`, 4 ticks, Book button), HV7 "what your treatment involves" (`#treatment`: 3 parts; part 1 has 2 flyer video cards, part 2 has 5 (electrotherapy, heat, ultrasound, deep tissue, therapeutic exercise), part 3 is a phone-style "home programme" video card built in HTML, static with no links and no progress bar because White has no app; Book button) and HV8 "patients treated in their own homes" (`#patients`: swipe carousel of 4 placeholder video cards with "Patient name / Condition, State", no stars until there are real reviews) HV9 team (`#team`: 4 placeholder cards, "Physiotherapist name", photos `assets/team-1..4.jpg`, plus an "And many more" card; never use Therafit's people) and HV10 how it works (`#how-it-works`: 3 steps; steps 1 and 2 describe the WhatsApp way and must be rewritten when the booking flow exists) done. HV11 "physiotherapy is usually the first step, not the last" (`#first-step`, conversion section: surgery card, then a deep blue home-physiotherapy card with the Book button inside it, an honest "some conditions do need surgery" line, and a green "Not sure? Ask us on WhatsApp" button; costs and timelines are words, not made-up figures, and every claim in it needs a physiotherapist's check) done. HV12 condition cards (`#start-here`: 12 cards as in Therafit's grid, each opens WhatsApp with the condition in the message via `data-condition`, plus a "not listed" button) and HV13 "what comes with every plan" (`#included`: 2 gifts + 6 WhatsApp bonuses, all placeholder) HV14 "which one is right for you?" (`#home-or-studio`: deep blue home card with Book button, white FlexRecovery Studio card with a link to `/studio` (page not built yet, phase 3) and the Ota / Osogbo pills, honest line, green "Not sure which is best? Ask us on WhatsApp") and HV15 "physiotherapy and pain medication" (`#medication`, with the "never stop your medicine" safety line and a Book button) done. HV16 "why patients choose White" (`#why-white`, 6 cards; "honest advice" replaces Therafit's payment card), HV17 FAQ (`#faq`, 10 questions, native `<details>`, FAQPage JSON-LD in the head must stay identical to the visible answers) and HV18 final call to action (`#start`, Book + WhatsApp buttons, placeholder disclaimer) done. **The home-visit page is COMPLETE: all 18 sections built.** Before launch it still needs White's real content: see the must-replace list below and every `data-placeholder` in `home-service.html` (photos, videos, team names and bios, made-up numbers, gift values, health claims for physiotherapists to check, disclaimer wording). Still open for this page: the booking flow (quiz, location, price, Book button) replaces the WhatsApp buttons via the last block of `js/main.js`, and steps 1 and 2 of HV10 and the first two FAQ answers must then be rewritten; `/studio` must exist because HV14 links to it.
- **No quiz, no price engine yet.** Every booking button carries `data-wa` and opens WhatsApp with `SITE.homeVisitMessage` (`js/components.js`); `data-wa="enquiry"` uses the general message; `data-condition="Knee pain"` adds the condition. The handler is the last block in `js/main.js`: it is the one place to change when the flow exists.
- **Future booking flow (Praise, not built):** short quiz with basic info only (no pain level, no deep questions) -> location selector with all 36 states (Lagos, Ondo, Ogun, Osun list a few areas each; every other state lists only its capital) -> the price White sets for that location -> a Book button (he will say what it does before we build it).
- **CTA wording:** never "get my plan and price on WhatsApp" or "see plan and price". Use "Book My Home Visit" (blue) and "Chat With Us on WhatsApp" (green). Header on this page shows a "Book a Visit" button next to the menu on phones (`<body data-header-cta="Book a Visit">`).
- **Facts from Praise:** White's main home-visit area is **Lagos**; they also do other states. White has **no home exercise app or page**. Where unsure, use Therafit's wording (rephrased) and tag it `data-placeholder`; he will change it later.
- **Placeholder numbers are allowed (Praise: "you can use numbers, we'll replace them").** Use numbers similar to Therafit's when a section needs them, tag them `data-placeholder`, add an HTML comment, and list them here so nothing fake reaches launch. **Still never add timers, countdowns, "slots left", live date chips, "batch" deadlines or other fake urgency.**
  - Must replace before launch: **HV13 gifts, bonuses and every "Worth ₦…" value plus "Total value ₦110,000"** (all Therafit's; White must confirm what it really gives, and the total must equal the sum of the values); trust strip "150+ patients treated at home" and "10 states covered" (`home-service.html`, HV2). Any more numbers added later go in this list.
- **Placeholder claim to confirm with White:** the bold line under the quotes ("These are not random words. They are the words of real Nigerians in pain.") was requested by Praise. Keep it only if the quotes really reflect what White's patients say.
- Left out on purpose: live date chip, "slots left", countdowns and any other urgency, struck-through prices, surgery costs, "2 to 4 weeks", money-back, gift values.
- The hero anchors `#home-visit-video` and `#patients` only work once those sections exist (give those sections exactly those ids).

- **Video card with a cover image:** `.video-card--flyer` (portrait). Put an `<img class="video-card__cover">` as its first child and that flyer shows on the card; delete the `<img>` to show the YouTube thumbnail instead. Paste the YouTube link or ID into `data-video-id`. Placeholder flyer: `assets/home-visit-flyer.jpg` (replace, same filename).

- **Swipe carousel (`.carousel`, built in HV7):** `<div class="carousel" data-carousel>` holding `<ul class="carousel__track" tabindex="0">` (slides are `<li class="carousel__slide">`) and `<div class="carousel__nav" hidden>` with `.carousel__btn--prev`, `.carousel__dots`, `.carousel__btn--next`. Swiping works without JavaScript; `js/main.js` adds arrows and dots and hides them when all slides fit. Reuse it for the team cards. Placeholder flyers: `assets/treat-*.jpg` (replace, same filenames).

## FlexRecovery Studio page (`/studio`, file `white-physio/studio.html`): DONE (all 9 sections built)
**Praise's layout brief (agreed):** a good, lean page built for **conversion and the visitor's journey**, only the sections that matter, in the existing branding. Most visitors will live near Ota or Osogbo and want to come to the clinic. Ota and Osogbo have **different WhatsApp messages** and the visitor picks either. A "can't come to the studio?" section sends people to the home visit. A section for inside/outside photos of both studios (White will send real photos later, placeholders now). Same number for both studios for now (+234 902 983 9464).
**Agreed section plan (9):** ST1 hero (logo, headline, two green buttons Ota / Osogbo, link to home visits), ST2 choose your studio (`#choose-studio`, cards `#ota` and `#osogbo`), ST3 what you get + who it is for, ST4 how it works (3 steps: message us, we confirm a time, you visit), ST5 inside our studios (carousel, photos labelled Ota / Osogbo), ST6 patient stories (video cards), ST7 can't come to the studio? (blue section, Book My Home Visit -> `/home-service`, link to the 30-day programmes), ST8 FAQ (6 questions + FAQPage JSON-LD in the head), ST9 final call to action (Ota + Osogbo buttons). The "Home vs Studio" comparison is left out here: it already lives on the home-visit page (HV14).
**Done so far:**
- **ST1 hero** (reuses `.hv-hero`; new `.st-hero__logo`, `.st-pick` with two green buttons, `.st-link`). Both buttons sit above the fold on phones. Small line under them: "Too far to travel? We can come to your home. See home visits" -> `/home-service`.
- **ST2 choose your studio** (`.st-locs` / `.st-loc`: photo, state, name, address, hours, "Message us first" note, green button).
- **ST3 what you get + who it is for** (`#what-you-get`, soft background): six `.hv-why-card` cards (licensed physiotherapists, full check-up, hands-on treatment, help to move again, exercises with guidance, support after your visit) and a `.st-for` card "Our studio is a good choice if you..." with 5 ticks. All placeholder wording; White must confirm "licensed", what treatment covers and WhatsApp support after visits.
- **ST4 how it works** (`#how-it-works`, white): three `.hv-steps` (message us, we agree a time, first visit) then the Ota / Osogbo buttons again (`.st-pick st-pick--center`). Rewrite the steps if White adds online booking.
- **ST5 inside our studios** (`#inside`, soft): one photo carousel per studio (`.st-gal`, three 4:3 slides each; all three fit on desktop so the arrows hide). Captions "Outside the studio / Treatment area / Exercise area" are placeholders. Add slides by copying an `<li class="carousel__slide">`.
- **ST6 patient stories** (`#patients`, white): the HV8 cards (`.hv-patient` + video card + swipe carousel), 4 placeholders (2 Ota, 2 Osogbo), no stars. Paste each YouTube link or ID into `data-video-id`; change "Patient name" and "Condition, Ota/Osogbo".
- **ST7 can't come to the studio?** (`#cant-come`, deep blue): two cards (`.st-alt__card`): "A physiotherapist at your home" (button "See Home Visits" -> `/home-service`) and "A 30-day programme from home" (button -> `/#programs` on the homepage). "Mainly in Lagos, and in other states too" is Praise's fact; the rest is placeholder wording.
- **ST8 FAQ** (`#faq`, soft): 6 questions (book, where, wear and bring, cost, referral, cannot travel), native `<details>`, plus the **FAQPage JSON-LD in the head, which must stay identical to the visible answers**. Answers are placeholders (what to bring, price rule, referral).
- **ST9 final call to action** (`#start`): the HV18 `.final-cta` block with the two studio buttons, a "Too far to travel? We can come to your home" link to `/home-service`, and the placeholder disclaimer. Background `assets/cta-team.jpg` (placeholder: replace with White's team photo, same filename).
- **WhatsApp:** `data-wa="studio-ota"` and `data-wa="studio-osogbo"`; the words live in `SITE.studioOtaMessage` / `SITE.studioOsogboMessage` (`js/components.js`), the lookup is the last WhatsApp block of `js/main.js`.
- **Header button on this page:** `<body data-header-cta="Find a Studio" data-header-cta-href="#choose-studio">`. `data-header-cta-href` (new) makes the header button a plain link instead of the home-visit WhatsApp message. Home-visit page is unchanged.
- CSS: section 26 at the end of `style.css` (26a hero, 26b studios, 26c what you get, 26d how it works, 26e gallery, 26f stories, 26g can't come cards, 26i final call to action; the FAQ needs no new CSS). JSON-LD in the head is a `Physiotherapy` entity with no address or hours (not confirmed yet; add them when White sends them).
- **Still open for this page:** when White sends the details, fill the two addresses and opening hours in ST2, add `address` / `openingHours` (one entry per studio) to the `Physiotherapy` JSON-LD in the head, and update the FAQ answer "Where are your studios?" if you add the addresses (keep the FAQPage JSON-LD identical). The header button "Find a Studio" uses `data-header-cta-href` (see above).
- **Must replace before launch (studio page):** the two addresses and the opening hours ("Monday to Saturday, 8am to 6pm" is made up) in ST2; placeholder photos `assets/hero-studio.jpg`, `assets/studio-ota.jpg`, `assets/studio-osogbo.jpg` and the six gallery photos `assets/gallery-ota-1..3.jpg`, `assets/gallery-osogbo-1..3.jpg` (same filenames, gallery 4:3); the gallery captions; every `data-placeholder` wording. Preview: `python3 tools/build_preview.py white-physio white-physio-studio-preview.html studio.html`.

### Original notes for this page
Source: `white-physio-brief-phase2-3.md` section 9 (in the project files) plus these notes. Start by telling Praise what you need, then WAIT for his "go" (same as the home-visit page).
- **Sections (from the brief):** studio introduction; what patients receive (professional physiotherapy, assessment, treatment, rehabilitation, guided exercise, recovery support); who it is suitable for; the studio experience; gallery; Ota; Osogbo; Home vs Studio; patient stories; FAQ; contact / WhatsApp.
- **Main CTA:** "Contact the Studio" (WhatsApp). No Google Maps at first. Use the FlexRecovery Studio "By WhitePhysio" logo (`assets/logo-studio.png`: pink name, blue byline).
- **Locations (Praise, still to confirm with the client):** Ota (Ogun State) and Osogbo (Osun State). Addresses, opening hours and gallery photos are not supplied yet: use tagged placeholders.
- **Already linking to `/studio`:** the homepage studio card and studio teaser ("Contact the Studio"), the home-visit page section HV14 ("See the FlexRecovery Studio") and its FAQ answer about no physiotherapist in my area. Keep their wording consistent with the studio page (placeholders: "guided sessions, with our team beside you").
- **WhatsApp:** DONE, see above (two messages, one per studio).
- **Reuse, do not rebuild:** `.video-card` (patient stories), `.carousel` (gallery), `.faq__item` (FAQ, plus FAQPage JSON-LD in the head), `.hv-why-card` cards, `.hv-opt` (home vs studio comparison), `.hv-cmp`, `.final-cta`, `.pill`, `.section--soft` / `.section--dark`. New CSS goes in a new numbered section at the end of `style.css`.
- Same rules as everywhere: simple words for elderly visitors, bold key phrases only, no fake urgency, placeholder numbers allowed only if tagged and listed in the must-replace list, every claim tagged `data-placeholder` until White confirms it, preview artifact plus zip on every delivery.
- Add a NEW preview artifact for the studio page (do not overwrite the home-visit preview).

## Rules from Praise (follow them on every page)
1. Build only when asked. If he asks "can you do X?", answer that first. Replies short: he reviews on a phone.
2. Every delivery = the **zip + a preview file**. Preview: `python3 tools/build_preview.py white-physio <out.html> [page.html]`, then publish it as an artifact (update the same artifact link when there is one).
3. Site users are mostly **elderly**, most do not read long text, and the goal is **conversion**. Use simple everyday words, short lines, and **bold key words/phrases** (`<strong>`). Bold only: no pink underline or marker highlight.
4. Look and structure stay the same as Therafit's. The **wording is rephrased** (never copied exactly) in simple words. Therafit copy is temporary; mark every placeholder with `data-placeholder` (or a `<!-- PLACEHOLDER -->` comment).
5. Do not reuse Therafit photos, flyers, videos or people. Photo slots use placeholders (`assets/*.jpg`); Praise replaces the files (same filename) or pastes an imgbb direct link into `src`.
6. YouTube links come from Praise later. Use the video card (below) with an empty `data-video-id`.
7. Do not add fake urgency (countdowns, timers, "slots left", fake "was" prices). Do not invent facts (ratings, registrations, guarantees). **Exception: placeholder numbers are allowed** (Praise will replace them) if tagged and listed in the must-replace list in the home-visit section above.
8. If a detail is not in the brief or the screenshots, ask for a screenshot instead of guessing. Check everything works and nothing breaks before delivering.
9. Do not put the FlexRecovery Studio in the conditions section.

## Facts on file
- Brand: White Physiotherapy Clinic, "Rehab | Fitness | Nutrition". Blue #004AAD, deep blue #002F70, accent pink #FF66C4 (badges, italic words on dark blue, ticks on dark). Fonts: Playfair Display (headings) and DM Sans (body).
- WhatsApp **+234 902 983 9464** is also used as the phone number for now. Email, address, opening hours, domain: still needed (placeholders in `js/components.js` -> `SITE`).
- Studio locations (Ota, Osogbo) are not confirmed with the client.
- WhatsApp buttons are green (`.btn--whatsapp`), everywhere.

## Previews: all pages link to each other (Praise's request)
- Every page has its OWN preview artifact (update in place, never make new ones): homepage https://claude.ai/artifact/BcidgnYH3voQFP6aBsooHx, home-visit https://claude.ai/artifact/3EC6nfgLswJsmG22yd87iy, studio https://claude.ai/artifact/CrJrTBDn3FqMN7eENZNPJx, Piriformis https://claude.ai/artifact/FYsJM7sEBkWMCVEhPkKKH2, Sciatica https://claude.ai/artifact/ALj5jAUejv3Ta8expBovq3, Cervical https://claude.ai/artifact/1K53eLun3SnwdN1E2v19rh, Knee https://claude.ai/artifact/BhcE3aQK5dpKz7D4Ci5Gvv, payment https://claude.ai/artifact/5p92YDvsw8MFsXPpdFGuqN.
- `tools/preview-links.json` maps each site path (`/`, `/home-service`, `/studio`, `/programmes/...`, `/payment`) to its artifact. `tools/build_preview.py` reads it and points every internal link of a PREVIEW at the right artifact (in a new tab, `target="_blank"`), including the footer links that the scripts build at run time. A link to the page itself stays in the page; `#anchors`, WhatsApp, tel and mail links are untouched; the build prints a WARNING for any internal link with no entry. **When a NEW page is added, publish its preview, add its path to that JSON, then rebuild and republish the other previews.** The real site files are unchanged: their links stay normal (`/home-service` etc.).
- In previews the registration form's "Preview only" note also shows a "See the payment page" button (`window.__PREVIEW_LINKS`, set only by the preview builder; the real site never has it).
- After changing any page, rebuild that preview: `python3 tools/build_preview.py white-physio <output.html> <page.html>` and publish it to its own artifact link above.
- Images: Praise will send images and say where each goes. Copy them to `white-physio/assets/` (resize and compress phone photos, keep the placeholder's file name when replacing one, or update the page's `src`), add real alt text and width/height, then rebuild the affected previews. Only photos White owns or has consent for; never Therafit's.

## Images added by Praise (homepage 30-day programme cards)
- Praise sent four photos; they now fill the four cards in the homepage "30-Day Home Exercise Programme" group (`index.html`, `#programs`): **sciatica** = man with the red glow along the spine and leg (file 1003915208), **piriformis** = man on a sofa holding his buttock with the red glow (1003915229), **cervical** = older woman holding her neck (1003915269.webp), **knee** = woman holding her knee (1003915240, a Getty preview, since replaced: see below). Saved over the placeholders as `assets/condition-sciatica.jpg`, `condition-piriformis.jpg`, `condition-cervical.jpg`, `condition-knee.jpg` (600 x 1067, about 45 to 80 KB each). No HTML change was needed (same file names; the images are decorative, `alt=""`, because each card has its heading).
- **Sizing recipe (use it for every new photo):** the card picture area is **9:16 portrait** (`.cond-card__media`, `object-fit: cover`, centred). Crop the source to 9:16 around the person and the painful area (full height, narrow width), resize to 600 x 1067 (Lanczos + a light unsharp mask), save JPEG quality about 84, keep the file name. For sharper results ask Praise for the biggest original: the ideal source is 1080 x 1920 portrait; a landscape source needs at least about 1200 px on the SHORT side to look crisp because only a narrow strip is used. Other slots: home-visit condition cards `visit-*.jpg` 600 x 1067 (9:16), pathway cards `pathway-*.jpg` 900 x 1100, hero `hero-*.jpg` 1000 x 750 (4:3), studio cards `studio-*.jpg` 800 x 500, gallery `gallery-*.jpg` 800 x 600 (4:3), treatment flyers `treat-*.jpg` and `home-visit-flyer.jpg` 640 x 800 / 800 x 1000 (4:5), team photos `team-*.jpg` 320 x 320 (square, centre the face), final call to action `cta-team.jpg` 1600 x 700.
- **Knee photo replaced (Praise, later):** the first knee photo (1003915240) was a Getty preview with a watermark. Praise asked to remove the watermark; I declined (it marks licensed stock) and asked for a licensed or own photo. He then sent **1003915350** (woman sitting on a road, hand on a glowing knee, 408 x 612, no watermark visible): cropped 9:16 (window x 18 to 362, full height) and saved over `assets/condition-knee.jpg`. **Flags:** (1) The knee card is no longer a Getty preview, but this photo also looks like stock: White must have the right to use it. (2) The source photos are small (the neck photo is only 480 x 253, the knee photo 408 x 612), so those cards can look a little soft on high-resolution phones; ask for larger originals. (3) The neck photo has no red glow (the other three do); White can supply one with a glow if wanted. (4) All four photos look like stock or web images: White must have the right to use them.

## Images added by Praise (homepage home-visit cards, second batch)
- Five photos for the home-visit cards (`index.html`, `#home-visit`), saved over the placeholders as `assets/visit-*.jpg` (600 x 1067, 9:16, about 40 to 90 KB each; same file names, no HTML change): **Elderly Mobility** = nurse with an older woman using a walker (file 1003915291); **Stroke Rehabilitation** = older woman raising purple hand weights with a helper behind her (1003915300); **Post-Surgery Rehab** = physio holding a man's raised leg on a treatment table (1003915301); **Shoulder & Hip Pain** = older woman holding her shoulder with a man's hand comforting her (1003915243); **Posture Correction** = physio guiding a seated woman's arm and shoulder (1003915311). Praise chose the posture and stroke pairings himself.
- **Flags:** (1) `visit-elderly.jpg` carries a faint **DREAMSTIME** watermark across the middle: it is stock; I did not remove or hide it (it is faint but visible on the card). White needs the licensed version or its own photo (same file name). (2) The other four look like stock or web images (one looks AI-generated): White must have the right to use them. (3) The photos are landscape, so the tall 9:16 crop shows only a narrow strip: the stroke photo loses the raised hand weights (the crop shows the two faces and the raised arm), and the post-surgery photo (only 588 x 321) is soft on sharp screens. **DONE (Praise said yes): the five home-visit cards are now 4:5** (`.cond-grid--visit .cond-card__media { aspect-ratio: 4 / 5; }`, images 600 x 750, HTML width/height updated). Only the old 600 x 1067 crops existed, so the new files are a vertical window cut from them (same width strip, less height: the elderly man loses his feet, the post-surgery photo loses the band end). **For a wider crop ask Praise for the original photos** and re-crop straight from them at 600 x 750. The programme cards stay 9:16. (4) Only Elderly, Stroke, Post-Surgery and Posture are about "people being helped"; none has the red pain glow (Shoulder & Hip Pain could use one).

## Images, third batch (Praise) + super-resolution clean-up (done)
- **Placed:** `pathway-home.jpg` = therapist treating a woman lying on a treatment table (Home Physiotherapy Visits card); `pathway-virtual.jpg` = woman doing a squat while following a workout on the TV (30-day programme card; crop keeps the TV and the woman); `condition-cervical.jpg` = man holding his neck (replaces the low-resolution older-woman neck photo); `visit-elderly.jpg` = older man bending to hold his knee on a park path (replaces the walker photo that carried the Dreamstime watermark: replaced, never edited); `visit-postsurgery.jpg` = woman with a knee brace and resistance band and a physio with a clipboard (replaces the small leg-raise photo); `visit-posture.jpg` = physio adjusting a woman lying on a treatment table (**replaces the seated-arm photo Praise had picked himself, because that one was only a 281 px wide strip; tell Praise, and revert if he prefers the old pick**).
- **Cleaned with an AI super-resolution model (EDSR via OpenCV, run in the sandbox, model files NOT shipped):** sciatica, cervical, elderly, stroke, shoulder and pathway-virtual. It gives cleaner edges than plain resizing but cannot add real detail. `condition-knee.jpg` (408 x 612 source) was NOT re-run and stays at standard quality.
- **Not used (still available in Praise's uploads):** man holding his lower back with a red glow (a good alternative for the Sciatica & Lower Back Pain card), patient walking on parallel bars with a therapist (good for Stroke Rehabilitation).
- **Flags:** (1) `visit-postsurgery.jpg` looks AI-generated (the original has a small Google Gemini sparkle mark; it is not inside the crop, and I did not edit anything out): White should decide whether an AI illustration is acceptable and not present it as a real patient. The sciatica photo is also AI-looking. (2) All photos look like stock or web images: White must have the right to use them. (3) `visit-elderly` no longer has a watermark because the photo was replaced, not edited. (4) pathway-studio.jpg is still a placeholder.
- **Context note for the next chat:** this chat got very long (many images, code and tool output are re-read on every message, which uses up the usage limit quickly). Start a fresh chat and read `HANDOFF.md` first; it holds the whole state.

## Files
- **Floating WhatsApp button** (`.wa-float`, CSS 10b): written by the footer script of `js/components.js`, so it is on every page automatically. 48px green circle, bottom right, no label or animation. It opens WhatsApp with `SITE.whatsappMessage`; a page can set its own message with `<body data-wa-message="...">` (the Piriformis page does). The back-to-top button sits above it. When the registration modal is built, hide the float while the modal is open.
- Logo-only header (programme pages): `<body data-header-logo-only>`; `components.js` then writes the header with the logo alone.
- `css/style.css` (section index at the top; one file for the whole site), `js/components.js` (header + footer, and the `SITE` settings: change contact details there once), `js/main.js` (menu, back-to-top, video cards, count-up numbers).
- Every page: `<script src="/js/components.js"></script>` right after `<body>`, `<main id="main">...</main>`, then `<script src="/js/components.js" data-part="footer"></script>` and `<script src="/js/main.js" defer></script>`. Copy the `<head>` from `index.html` (fonts, JSON-LD) and change title, description, og tags.
- `_headers`, `robots.txt`, `llms.txt` in the site folder. `styleguide.html` is a dev page (noindex): delete before launch.

## Components (all in style.css)
- Layout: `.container`, `.section` (`--soft`, `--tint`, `--dark`), `.section-head` (centred pill + h2 + p).
- `.pill` (`--dot`, `--on-dark`, `--sm`, `--block`, `--outline`, `--accent`, `--serif`).
- `.btn` + `--primary`, `--outline`, `--outline-soft`, `--light`, `--ghost`, `--outline-light`, `--whatsapp`, `--block`, `--lg`, `--sm`, `--pill`.
- Cards: `.cond-card` in `.cond-grid` (photo + title + text, hover overlay), `.prog-card`, `.price-card` (`--featured`), `.why-card`, `.path-card`.
- Lists: `.ticks` (`--brand`, `--accent`, `--light`), `.icon-list`, `.tag-list`.
- FAQ: native `<details class="faq__item">` (works without JS). Keep FAQPage JSON-LD in the head identical to the visible answers.
- Video card: `<div class="video-card" data-video-id="">` with `.video-card__btn`; accepts a YouTube link or 11-char ID; shows "Video coming soon" when empty; loads the player only on tap. Stars: `.stars`.
- Numbers that count up: `<span class="cnt" data-to="10000" data-comma>10,000</span>` (real number stays in the HTML).
- Notice band `.notice`, final call to action `.final-cta`, utilities `.sr-only`, `.italic`, `.text-brand`, `.text-deep`, `.text-accent`, `.nowrap`.

## SEO and speed (keep doing)
- One `<h1>` per page, no skipped heading levels, unique `<title>` (about 60 characters) and meta description (about 150) per page.
- Images: real `width`/`height`, `loading="lazy" decoding="async"` below the fold, compressed (about 150 KB or less).
- Structured data per page (JSON-LD): programme pages get their own; FAQ blocks get FAQPage. Only true facts.
- Fonts load without blocking (already in the head). No render-blocking extras.
- **DONE: header and footer are now baked as static HTML into every page** (`tools/bake_header_footer.py`, mirrors the exact markup `js/components.js` used to write at runtime). `components.js` still runs on every page (it sets `window.WHITE_SITE`, used by `main.js` and `js/booking.js`), but now only inserts header/footer via JS as a fallback, guarded by `if (!document.querySelector('.site-header'))` / `.site-footer` — so it will still work on any future page that isn't baked yet. If `SITE` (name, WhatsApp number, phone, email) or the footer nav links in `components.js` change, re-run `python3 tools/bake_header_footer.py` to refresh the baked copies in all 8 pages, or they will drift out of sync.
- **Still to do at launch:** domain -> canonical, `og:url`, `og:image`, `sitemap.xml`, sitemap line in `robots.txt`; real phone/email/address/hours in `SITE` and the JSON-LD (re-run the bake script after); replace all Therafit-derived copy with White's own (duplicate content hurts ranking); switch on image caching in `_headers` once photos are final; delete `styleguide.html`.

## Programme page reminders (from the brief)
- One template, data per programme (slug, hero lines, 5 quotes, solution, phases, included list, for/not-for lists, FAQ, videos, price). One shared registration modal with a hidden field holding the programme name. Payment page after it (static bank details for now).
- Leave out the deferred urgency items (countdown, "slots left", "CURRENT BATCH ONLY", struck-through price, "50% OFF") and any money-back claim unless White confirms it.

## Pre-deploy audit (this chat): WhatsApp text, SEO basics, broken links
- **WhatsApp pre-filled messages:** read through every `wa.me` link across all 8 pages. All use the same number (2349029839464), all driven centrally from `SITE` in `js/components.js` (`whatsappMessage`, `homeVisitMessage`, `studioOtaMessage`, `studioOsogboMessage`, plus per-programme and per-page `data-wa-message` overrides). No inconsistencies or typos found. Nothing changed.
- **Alt text:** 16 `<img>` tags across `index.html`, `home-service.html` and `studio.html` had `alt=""` (fine for a background image, not fine for a real photo). Filled all of them in with descriptive text matching the surrounding placeholder comments (pathway cards, condition cards, home-visit cards, the clinic logo, and the `cta-team.jpg` used on 3 pages). No visual change, just markup.
- **Favicon, titles, meta descriptions:** already done on every page from earlier work (unique `<title>`, meta description, og tags, `/assets/favicon.png` + apple-touch-icon). Nothing missing.
- **Added `404.html`:** reuses the baked header/footer, `noindex, follow`, links back to home, home visit, programmes, and studio, plus its own WhatsApp float message. Netlify serves this automatically for any unmatched path with zero config.
- **Added `sitemap.xml`:** homepage, `/home-service`, `/studio`, all 4 programme pages. Deliberately leaves out `styleguide.html` (already `Disallow`'d in `robots.txt`) and `payment.html` (already `noindex, nofollow`, and it's a mid-flow step, not a landing page). **Still uses the `https://YOUR-DOMAIN` placeholder** — same placeholder `robots.txt` already had a commented-out `Sitemap:` line for. Once the real domain is live: find-and-replace `YOUR-DOMAIN` in `sitemap.xml`, and uncomment + fill in the `Sitemap:` line in `robots.txt`.
- **Broken-link pass:** every internal `href` across all pages resolves — homepage anchors (`#services`, `#programs`, `#conditions`, `#faq`) all have matching `id`s, and every `/home-service`, `/studio`, `/programmes/...` link points to a real page. These are extension-less URLs relying on Netlify's default "pretty URL" behavior (it auto-serves `foo.html` at `/foo`) — this is Netlify's default and needs no `_redirects` file, but worth clicking through once after the real deploy to confirm nothing's off.

## TeleRehab restructure + home-visit simplification + video-ID system (this chat)

**TeleRehab (was "30-Day Do It Yourself Programme"):**
- Renamed everywhere it's a label: page titles, meta/OG/Twitter tags, JSON-LD, hero pills, pricing cards, homepage sections, footer blurb, 404 page, studio FAQ.
- Registration form **removed completely** (the `<dialog id="register">` modal is gone from all 4 programme pages). Every "Register Now" style button now links straight to the EHR booking page (`SITE.ehrBookingLink` in `js/components.js`, currently `https://physio.flowvance.co/therapist-profile.html?slug=clinic93`), opening in a new tab, with a WhatsApp fallback next to it. **Payment happens on the EHR page, not here.**
- `payment.html` **deleted**. Nothing links to it anymore (checked sitewide). The dead JS that used to handle the registration form and the payment page was removed from `js/main.js` (main.js is ~270 lines shorter).
- 30-day money-back guarantee **removed everywhere** on programme pages (the whole "Zero risk" section, pricing tick, trust tile, decision-section mention, testimonial small-print).
- Text that specifically claimed "videos are sent to you" was reworded to describe **live TeleRehab sessions** instead (hero paragraph, "how it works" wording, the free-sample-video section, FAQ answers about being busy/elderly, pricing ticks). **Left untouched on purpose, per Praise:** the Phase 1/2/3 day-by-day curriculum content (Days 1–10, 11–20, 21–30) — that's "what's covered," not "how it's delivered," and Praise said to leave it until he confirms it's changing.
- Two emails now shown everywhere in the footer: `whitephysiotherapy@gmail.com` and `admin@whitephysiotherapy.com` (was a single placeholder `hello@example.com`).
- **Still open, per Praise, before this is really final:** confirmed session frequency (2x/3x/4x a week — currently just says "a few sessions a week" without committing to a number), whether it's still framed as a "30-day" arc, and the ₦15,000 price (he said he'd reconfirm).

**Home-visit page (`/home-service`):**
- Booking quiz cut from 4 steps down to **2**: location (state + area) → price. The old "your details" (name/WhatsApp/phone) and "what's troubling you" (condition) steps are gone — the EHR page collects that instead.
- The price step's main button now actually does something: it opens the EHR booking link in a new tab (previously a placeholder that did nothing). WhatsApp is offered at both steps.
- `js/booking.js` was rewritten (smaller, no name/phone/condition logic). The 12 condition cards and every other "Book My Home Visit" button still open the same dialog; they just don't pre-tick a condition anymore (there's nothing to tick).
- "What comes with every plan" section: removed the herbal tea, smoothie and bone broth guide cards (Praise's instruction). Kept TENS electrodes, lumbar gel, the food plan eBook, the decompression video and daily WhatsApp support. Total value recalculated: ₦110,000 → **₦65,000**.

**FlexRecovery Studio:**
- Real addresses added: **Ota** — beside MRS Filling Station, ILO Awela Road, Toll Gate, Ota, Ogun State. **Osogbo** — behind Kabelo Filling Station, Ota Efun, Osogbo-Ikirun Road, Osogbo, Osun State.

**Video IDs are now a single file — `js/videos.js`:**
- Every video on the site (40 of them: 3 homepage testimonials, 13 on the home-service page, 4 on the studio page, and 5 per programme page × 4 conditions) now has a plain-English label in one place. To add a video, open `js/videos.js`, find the label, paste the YouTube link between the quotes, save. Nothing else needs to change — no HTML editing, no touching `data-video-id`. This can be edited directly in GitHub's web UI.
- Under the hood: each `.video-card` now carries `data-video-key="some-label"` instead of an empty `data-video-id=""`. `js/main.js` looks the key up in `window.WHITE_VIDEOS`. A card can still take a direct `data-video-id="..."` if that's ever more convenient — it wins over the key if both are set.
- Studio and gallery **photos** were already using the "same filename, different file" convention (e.g. replace `assets/studio-ota.jpg` with the real photo, keep the name) — that part didn't need changing, it was already exactly this kind of no-code swap.

**Still pending (not built yet, waiting on Praise):**
- New condition pages: Carpal Tunnel/De Quervain Syndrome, Erb's/Injection Palsy, Plantar Fasciitis — will be built once the TeleRehab template settles, using this template as the base, with content I generate for review by White's physiotherapists before launch.
- Confirmed session frequency, "30-day" framing and price for TeleRehab (see above).
- Founder's name for the third team bio (Founder/Lead Physiotherapist, bearded man in blue suit) — team content for Oyindamola Odukogbe and Anthony Ifeanyichukwu Ifedigbo (photo cleaned up) is ready; home-service team grid still needs collapsing from 4 slots to 3.
- 3 new FAQ entries from White (surgery timing, assessment intensity, what to bring) — not yet placed.
- More instructions Praise said he'd send.

## Home-visit "Get Your Personalised Plan" rebuild (this chat)
- New prominent CTA band added right after the hero on `/home-service` ("🎁 3 steps · Get Your Personalised Treatment Plan and Price") — opens the same booking dialog as every other Book button.
- Booking dialog expanded from 2 steps to 3: **location → what's troubling you (12 conditions, single choice) → price**. The condition step is collected for White's team only — it never changes the price shown.
- **Real pricing wired in** (`SITE.homeVisitPricing`, `js/components.js`), from White: two tiers, not per-state —
  - Lagos / Abuja / Rivers: ₦45,000/session (10% off ₦50,000) — ₦360,000 for 8 sessions, ₦540,000 for 12
  - Everywhere else: ₦35,000/session (₦5,000 off ₦40,000) — ₦280,000 for 8 sessions, ₦420,000 for 12
  - Package totals aren't hardcoded — they're computed as rate × session count in `js/booking.js`, so changing the 4 numbers in `components.js` is the only edit ever needed.
- The price step shows the full breakdown: per-session rate, 8-session package total, a ₦25,000 mobilisation fee, then a 70%/session-1 + 30%/session-5 split, a 12-session alternative (total only, no invented instalment terms for it), and a small "Surgery ₦4M+ vs Your Plan" comparison box. **PLACEHOLDER, unconfirmed for White:** the ₦25,000 fee and the 70/30 split are Therafit's structure, used as a stand-in per Praise's request — the per-session rates above are White's real numbers, the fee/split are not yet confirmed.
- The surgery-vs-home-physiotherapy comparison section (further down `/home-service`) now uses Therafit's exact figures as placeholders (₦4,000,000–₦8,000,000+ hospital bills, 6–12 months recovery, 2–4 weeks typical improvement, etc.) instead of the previous deliberately-vague wording — flagged in the file for White's physiotherapists to confirm before launch.

## Team (not yet placed)
5 team members' content is ready but **not yet added to the page**: Oyindamola Odukogbe, Anthony Ifeanyichukwu Ifedigbo (photo cleaned up), E. Tobiloba Adebayo DPT (Founder/Lead Physiotherapist), Adunola Samuel PT, Esther Adunola PT. The home-service team grid needs expanding from 4 photo slots to 5 and all 5 bios/photos added — next task for the next chat.

## Booking dialog audit and fix (next chat after "Get Your Personalised Plan")
- Tested in a headless browser (390px phone width): all 9 state/area combinations checked against White's numbers. Lagos / Abuja (FCT) / Rivers = ₦45,000 per session, ₦360,000 for 8, ₦540,000 for 12; every other state = ₦35,000 per session, ₦280,000 for 8, ₦420,000 for 12. Placeholder split adds up exactly (fee + 70% + 30% = package total). Validation messages, Back (keeps choices), Esc, the Book button (opens the EHR link) and the WhatsApp message (area + condition) all work. 25 buttons on the page open the dialog.
- **Fixed (CSS section 31, end of `style.css`):** the location and condition pills on the price step were white text on a white background (invisible); the breakdown labels were centred instead of left-aligned; the "12 sessions" box squeezed its text.
- All 8 pages: HTML tags balanced, no duplicate ids, no broken links / anchors / images, JSON-LD valid, no console errors, no sideways scroll on phone or desktop.
- Team section still NOT built (5 members ready, see above).

## Fixes and team (latest chat)
- **Preview CTA opened WhatsApp instead of the form:** buttons with an empty `data-wa` now get `href="#book"` from `js/main.js` (no WhatsApp link, no new tab) and `js/booking.js` listens in the capture phase, so the dialog opens even in in-app viewers. WhatsApp is still offered inside the dialog.
- **Leaked placeholder text under the CTA band:** a comment had lost its opening `<!--` in `home-service.html` (before the trust strip). Fixed.
- **"about 60 seconds" removed** from the CTA band pill, the dialog header and the step notes (`booking.js`).
- **Team built** (`#team`, 5 cards + "And many more"): E. Tobiloba Adebayo DPT (team-3.jpg), Oyindamola Odukogbe (team-1), Anthony Ifeanyichukwu Ifedigbo (team-2), Adunola Samuel PT (team-4), Esther Adunola PT (team-5, new file). Bios are simple-words versions of White's info. Real photos now in `assets/team-1..5.jpg` (480 x 480). All 5 photo matches confirmed by Praise: team-1 Oyindamola (grey background), team-2 Anthony, team-3 Tobiloba (blue suit), team-4 Adunola Samuel (man, green scrubs; bio uses he/his), team-5 Esther (yellow background).
- **Still not built:** the 3 extra condition pages (Carpal Tunnel / De Quervain, Erb's / Injection Palsy, Plantar Fasciitis), the 3 new FAQ entries (surgery timing, assessment intensity, what to bring).

## Three extra condition pages + three FAQs (latest chat)
- **New pages** (same template and sections as the knee page): `/programmes/carpal-tunnel-de-quervain`, `/programmes/erbs-palsy-injection-palsy`, `/programmes/plantar-fasciitis`. They are GENERATED by `tools/make_condition_pages.py` from `programmes/knee-arthritis.html`: edit the wording in the `PAGES` list in that script and run `python3 tools/make_condition_pages.py` (it overwrites the three files, so do not hand-edit them). ALL wording is placeholder written for review: White's physiotherapists must check every health statement, red-flag line and "do not book if" rule. Price kept at the ₦15,000 TeleRehab placeholder, no guarantee, no urgency.
- Wired in: homepage condition cards (now 7) and TeleRehab cards (now 7), footers on every page + `js/components.js` fallback lists, `sitemap.xml`, `llms.txt`, and 15 new video keys in `js/videos.js` (carpal-, erbs-, plantar- sample + testimonial 1 to 4).
- **PLACEHOLDER photos** for the three new homepage cards: `assets/condition-carpal.jpg`, `condition-erbs.jpg`, `condition-plantar.jpg` (blue "Photo coming soon" images, 600 x 1067). Replace with real photos, same names.
- **FAQs on /home-service** (now 13): added "What should I bring or prepare for my first visit?", "How detailed is the first assessment?", "When should I think about surgery instead?" (placeholder answers: White never sent its own wording, replace when it does). Also rewrote the first FAQ answer, which still described the old name-and-number step. JSON-LD FAQPage regenerated to match.
- Preview artifact links for the new pages are in `tools/preview-links.json`.

## Homepage FAQ, hero logo, condition photos, programme-page teams (latest chat)
- **Homepage FAQ** (`index.html`) now has the same 3 questions added earlier to `/home-service`: "What should I bring or prepare for my first visit?", "How detailed is the first assessment?", "When should I think about surgery instead?" (9 items total). JSON-LD FAQPage regenerated to match.
- **Hero watermark logo was off-centre on desktop** (only visible at 900px+, which is why it wasn't caught on phone review): it was using the full text lockup (`logo-clinic.png`), which is wide and left-heavy, so it looked shoved to one side inside its circle. Swapped to the icon-only mark (`logo-icon.png`, the file the brief reserves for watermarks and favicons) and resized it to sit centred. Nothing else about hero changed.
- **Condition-card photos:** the 3 new TeleRehab cards on the homepage (Carpal Tunnel, Erb's/Injection Palsy, Plantar Fasciitis) now use real stock photos supplied by Praise (red glow on the painful area, matching Therafit's style: a wrist, a child's shoulder, a foot). These are STOCK photos, not White's own patients — replace with White's real photos before launch, same file names (`condition-carpal.jpg`, `condition-erbs.jpg`, `condition-plantar.jpg`, all 600 x 1067).
- **Programme-page team section:** all 7 programme pages (Sciatica, Piriformis, Cervical, Knee, Carpal Tunnel, Erb's/Injection Palsy, Plantar Fasciitis) had a generic "[Name]" x 3 placeholder team block with invented roles (nutrition expert, herbal specialist) inherited from Therafit. Replaced on all 7 with the 3 confirmed team photos and real names: E. Tobiloba Adebayo DPT (Founder & Lead Physiotherapist), Adunola Samuel PT, Esther Adunola PT. Dropped the nutrition/herbal specialist roles since White has not said it offers those. If White wants Oyindamola or Anthony listed here too (only 3 rows fit the current layout), say so and I will swap or extend it.

## Confirmed items cleaned up + free gifts trimmed + privacy policy + domain wired in (latest chat)
- **Payment page:** confirmed there isn't one anymore (was deleted in an earlier session; TeleRehab and home-visit both book through the external EHR page). An earlier reply in this chat wrongly listed it as still needing bank details -- that was a mistake, now corrected.
- **Confirmed by Praise (2026-09-25), stale "unconfirmed" comments removed and `data-placeholder` cleared on the affected elements:** the ₦15,000 TeleRehab price (all 7 programme pages), the CAC-registered/licensed-physiotherapist trust tiles (all 7 programme pages), the surgery-cost comparison on `/home-service` (HV11: ₦4M+, 6-12 months, etc.). The money-back guarantee was already removed sitewide in an earlier session -- just cleaned up the leftover comments that still mentioned it as pending.
- **Free gifts trimmed to match `/home-service`:** the "Everything included" section (PG7) and the pricing-card tick list (PG9) on the 4 original programme pages (Sciatica, Piriformis, Cervical, Knee) still listed the old Therafit gift set -- anti-inflammatory tea guide, bone broth guide, smoothie guide, weight-loss tea guide. Removed all of these from all 4 pages, in both places they appeared. What's left on every programme page now: Live TeleRehab sessions, Weekly WhatsApp follow-up, the condition's food-plan eBook+video, the posture/movement video, and (where the page already had it) daily WhatsApp reminders. The 3 new pages (Carpal Tunnel, Erb's/Injection Palsy, Plantar Fasciitis) were written from scratch and never had this content, so nothing to trim there.
- **Privacy policy built:** new page at `/privacy` (`privacy.html`), linked in the footer of all 11 pages, added to `sitemap.xml`. Written to match how the site actually works (WhatsApp, the EHR booking page, health info collected for home visits/TeleRehab), references the Nigeria Data Protection Act 2023. A few specifics are bracketed placeholders White needs to answer: where records are kept and who can access them (section 5), how long records are kept (section 6), any other tools/software used (section 4). **Recommend a Nigerian data-protection advisor or lawyer reviews it before it goes live** -- I'm not one, and this is a legal document.
- **Domain confirmed: whitephysiotherapy.com.ng.** Added `<link rel="canonical">`, `og:url` and `og:image` to all 10 real pages (not styleguide). Made a simple placeholder `og-image.jpg` (branded blue background with the icon mark) since there wasn't a real one yet -- swap it for a proper social-share image whenever White has one, same file name, 1200x630. Updated `sitemap.xml` and the `Sitemap:` line in `robots.txt` to the real domain.
- **Caught and fixed a real bug:** re-running the old `tools/bake_header_footer.py` script (to add the new Privacy Policy footer link) duplicated the header and footer on `index.html`, `home-service.html` and `studio.html`, because the script isn't safe to run on already-baked pages -- it doesn't check first. Caught it before finishing, manually removed the duplicate blocks (verified byte-for-byte against a fresh audit), and **fixed the script itself**: it now checks for the "already baked" comment marker and skips a page instead of double-inserting. Added the Privacy Policy link to the footer directly (safe find-and-replace across all 11 pages) instead of relying on the script. The script also had a stale single-email/4-programme footer template left over from before the TeleRehab restructure -- patched the email constant and left a note, though the added guard means this is unlikely to matter again.
- Re-audited and re-tested all 11 pages (phone + desktop, zero errors, zero overflow) after every change in this batch.

## Domain correction (latest chat)
- Praise corrected the domain: it's **whitephysiotherapyclinic.com.ng**, not whitephysiotherapy.com.ng (which was given in the previous message). All 57 references across the site fixed: canonical links and og:url/og:image on all 10 real pages, sitemap.xml (12 URLs), robots.txt's Sitemap line. Checked with a full-site grep afterwards -- zero references to the wrong domain remain anywhere. Re-audited and browser-tested all 11 pages again after the fix, still clean.

## Second email corrected to match the real domain (latest chat)
- admin@whitephysiotherapy.com -> admin@whitephysiotherapyclinic.com.ng, everywhere: all 12 baked footers (every page including 404) and the js/components.js source of truth. Checked with a full-site grep afterwards, zero old references remain. Re-audited and browser-tested all 11 pages again, still clean.

## Home-service hero copy update from White (latest chat, 2026-09-26)
- Headline changed: "Get Relief From Pain" / "In Your Home or Office" (was "...Right in Your Own Home").
- Hero paragraph replaced and made bold (CONFIRMED copy, not placeholder): "Before you pay ₦6,000,000 for surgery, or keep using drugs that can eventually damage your kidneys, let a physiotherapist come to your home or office." New CSS class `.hv-hero__text--strong`.
- Added a new 4-point checklist right under that paragraph (new `.hv-hero__points` class, checkmark style matching the site's other tick lists), CONFIRMED copy: Comprehensive on-site neurological & musculoskeletal assessment / Tailored mobility, strength, and fall-prevention protocols / Dedicated 1-on-1 care from licensed physiotherapists / Full guidance for family members & caregivers.
- Left untouched on purpose: the pill ("Licensed physiotherapy care at home"), the 3 short chips (No waiting in queues / No travelling in pain / The clinic comes to you), and the licence-check note -- Praise only asked for the headline + paragraph + new points, nothing else in the hero.
- Re-audited and browser-tested the whole site (all 11 pages, phone + desktop) after this change -- clean.

## Home-service hero: fixed a mistake from the previous entry, plus a batch of 10 client changes (latest chat, 2026-09-26)
- **Corrected the hero mistake:** the "before you pay ₦6,000,000..." line is now the actual **H1** (was wrongly left as a separate paragraph under a different headline). "Get relief from pain, at your home or office" is now the small lede line above the H1 (new `.hv-hero__lede` class). The 4-point checklist was merged into the original 3-chip row as 7 pills in one list (`.hv-hero__chips`), not a separate `.hv-hero__points` list -- that class is now unused dead CSS, left in place, harmless.
- **"Mainly in Lagos" removed everywhere** (hero `.hv-hero__where`, a team member's coverage tag, a dark-card bullet, the home-service FAQ, the assessment's location-step lead, the studio page body copy + its FAQ, both JSON-LD `"text"` fields): now "available nationwide" / "we offer home visits nationwide". Also updated to match: home-service `<title>`/meta/og/twitter (now "...in Lagos & Nationwide", kept "Lagos" as a search keyword since it's still a major market) and its JSON-LD `areaServed` (now just `{"@type":"Country","name":"Nigeria"}`, dropped the singled-out Lagos State entry).
- **Mobilisation fee: ₦25,000 -> ₦30,000.** Changed the one constant it's computed from (`site.homeVisitMobilizationFee` in `js/components.js`, plus the `js/booking.js` fallback default) -- the 70/30 split, balances and totals on the price step all recalculate from it automatically, nothing else to touch.
- **Founder's surname corrected: Adebayo -> Adetayo**, sitewide (home-service.html x3, all 7 programme pages x1 each).
- **Second email: admin@ -> admin1@whitephysiotherapyclinic.com.ng**, everywhere (all baked footers incl. 404, + `js/components.js` source of truth).
- **TeleRehab price reworded to "starts at / from ₦15,000"** (was presented as a flat price): homepage price card, and on all 7 programme pages -- the price-card price, the book-button label, the "invest ₦15,000" line (now "at least ₦15,000"), the "you do not have ₦15,000" line (now "at least"), and the testimonial-section price caption.
- **Studio opening hours: 9am-5pm**, both Ota and Osogbo (was 8am-6pm; "Monday to Saturday" left as-is, only the hours were given).
- **Google Maps links added** for both studio addresses: a small "View on Google Maps ↗" link (new `.st-loc__maplink` class) under each address, using a `google.com/maps/search` URL built from the address text already on the page. Opens in a new tab.
- **"Payment stroke" (Praise's client asked for red on the price-step payment; neither of us was sure what it meant):** made a judgment call -- highlighted the "Commitment & Mobilisation Fee" row in the price table (the one line that's actually paid now, vs. the other informational rows) with the site's existing `--danger` red tokens (left stripe + tint), and coloured the fee amount in the note below it the same red. If this isn't what they meant, easy to move elsewhere.
- **SEO pass:** audited titles/meta/canonical/OG/twitter-card/h1/alt-text/sitemap.xml/robots.txt across all 13 pages -- all already in good shape from earlier sessions (unique per page, FAQPage JSON-LD already present everywhere, alt="" on avatar thumbnails is correct since the name is already visible text next to them, `"@type":"Physiotherapy"` on the provider is a real, valid schema.org value, verified against schema.org directly). Added the one clear gap: **BreadcrumbList JSON-LD** on all 10 real pages (Home / Home > Home Visits / Home > FlexRecovery Studio / Home > Programmes > [condition]), which can make search results show a breadcrumb trail. Bigger, open-ended SEO work (keyword-targeted copy rewrites, content depth) is a separate task if White wants to scope it later.
- Re-audited (HTML balance across all 13 pages, JS syntax, JSON-LD validity) after every change in this batch -- clean throughout. Republished preview artifacts for the pages touched (home-service, studio, homepage, all 7 programme pages, privacy) at their existing links in `tools/preview-links.json`.
